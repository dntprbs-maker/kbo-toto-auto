# 🔍 KBO 스포츠 토토 배당률 검색 기능 리서치 보고서

대시보드 4번째 목표인 **"스포츠 토토 배당률 검색 기능 추가"**를 구현하기 위해 필요한 데이터 수집처를 인터넷 전역을 검색하여 찾아냈습니다. 다음과 같이 두 가지 접근 방식이 가능합니다.

---

## 💡 옵션 A: 글로벌 공식 API 연동 (가장 추천)

세계적인 배당률 제공 API인 **The Odds API**를 활용하는 방법입니다.

* **지원 여부**: KBO 리그를 공식 지원합니다. (API Sport Key: `baseball_kbo`)
* **제공 데이터**: 승무패(Moneyline), 핸디캡(Spreads), 언더오버(Totals) 등 글로벌 주요 북메이커(Pinnacle, Bet365 등)들의 실시간 KBO 배당률을 JSON 형식으로 제공합니다.
* **장점**: 
  * 서버에서 별도로 파싱하거나 크롤러를 짤 필요 없이, 깔끔한 JSON 형태로 바로 리액트/Vercel API에 붙일 수 있습니다.
  * 서버 아이피가 차단당하거나 에러가 날 확률이 거의 0%에 가까워 운영이 쾌적합니다.
  * 월 500회 호출까지 무료이므로, 개인 대시보드 프로젝트용으로는 비용이 발생하지 않습니다.
* **단점**:
  * 제공되는 배당률이 한국 공식 '베트맨(Betman)'의 프로토 승부식 배당률과는 소수점 단위에서 약간 차이가 날 수 있습니다. (흐름 및 정배/역배 기준으로는 동일)

---

## 💡 옵션 B: 국내 공식 배당률 크롤링 (차선책)

베트맨과 완벽하게 똑같은 한국 공식 "프로토 승부식" 배당률이 반드시 필요할 경우 사용하는 방법입니다.

* **크롤링 타겟**: 
  * `Wisetoto(와이즈토토)` 
  * `엔트리(Ntry)`
  * (베트맨 공식 사이트는 봇 차단 솔루션(Incapsula/WAF)이 강하게 적용되어 있어 단순 접근이 막힙니다)
* **방식**: Node.js의 `Puppeteer` 또는 Python의 `Selenium`과 같은 브라우저 자동화 도구를 띄워 페이지 내용을 읽어온 후(HTML 파싱) DB에 저장.
* **장점**: 대표님이 실제로 베팅하시는 "프로토 승부식"의 정확한 한국 배당률 수치를 그대로 가져올 수 있습니다.
* **단점**: 
  * 대상 사이트가 구조를 바꾸거나 봇 차단 기능을 켤 경우 크롤링 로직이 즉시 고장납니다.
  * 서버(Vercel 등)에서 지속적으로 브라우저를 띄워야 하므로 유지보수 및 서버 세팅이 API 연동보다 까다롭습니다.

---

### 👨‍💻 결론 및 개발자 추천
안정적이고 에러 없는 자동화를 원하신다면 **옵션 A (The Odds API)**를 사용하여 KBO 실시간 배당을 띄워두는 것을 추천해 드립니다. 

만약 배당률의 미세한 소수점까지 한국 공식 토토(베트맨)와 100% 동일해야만 분석에 의미가 있다고 판단되신다면, Vercel 백엔드 쪽에 크롤러를 구축하는 **옵션 B** 로 진행하겠습니다. 문서 확인하시고 방향을 결정해 주시면 바로 작업에 착수할 수 있도록 준비해 두겠습니다!
