# 📝 KBO TOTO 프로젝트 통합 작업일지

이 파일은 앞으로 진행되는 모든 프로젝트 작업 내역을 세부적으로 기록하는 통합 문서입니다. 최신 작업 내역을 상단에 추가하여 관리합니다.

---

## 📅 2026년 06월 24일 — 파이프라인 분리 / 데이터 정상화 / 배당률 진단

### 1. 작업 환경 세팅
* GitHub 레포(`dntprbs-maker/kbo-toto-analysis`)를 로컬(`C:\Users\크린\Desktop\claude 프로젝트\kbo-toto-analysis`)에 새로 클론하여 작업 폴더 구성.

### 2. 자동 파이프라인을 "단계별 수동 실행"으로 분리 (한 번에 안 돌 때 어디서 막혔는지 보기 위함)
* **0단계(KBO 일정 가져오기) 신규 추가**: 오늘 경기 결과 저장 + 내일 예정 경기 확인 기능.
* 관리자 화면(`AdminDashboard.jsx`)을 **카드형 → 가로 리스트형(0~5단계)** 으로 개편.
* 각 단계 실행 시 **팝업(alert) 대신 버튼 아래에 결과를 직접 표시**(성공=초록/실패=빨강, 경기 내역까지 글자로 노출).

### 3. ⚠️ Vercel 무료 플랜 함수 12개 제한 초과 → 배포 실패 해결
* **원인**: 0단계 파일 추가로 API 함수가 13개가 되어 배포가 막힘(대표님이 "12개 이상이면 안 된다"고 정확히 지적).
* **해결**: 흩어진 sync 4개 파일(`sync-schedule`, `sync-ai`, `sync-merge`, `sync-bets`)을 **`sync.js` 하나로 통합**(action 파라미터로 단계 분기). 검증된 버그수정 로직은 그대로 복사 이전.
* **결과**: 13개 → **10개**로 축소(여유 2칸 확보). 프론트 호출 경로도 `/api/admin/sync` + `{action}` 으로 변경.

### 4. 초롱이(AI 3) GPT 호출 실패(429) 해결
* **원인**: DB(`ai_models`)에 저장된 초롱이 모델값이 `gpt-4o`가 아니라 옛 잔재값 `gemini-2.0-flash`(존재하지 않는 모델)로 박혀 있어 429 연발.
* **해결**: 대시보드 **"AI 모델 성향 설정"** 에서 초롱이 엔진을 **GPT-4o로 다시 선택 후 저장** → DB 값 정상화. 이후 3단계 정상 작동(`초롱이 예측 완료 - 5경기`).
* 참고: 1·2단계(반짝이/별이 = 제미나이)는 사용량(429) 문제로 잠시 보류. 현재 GPT 1개만으로 운영.

### 5. 메인/모바일 화면 표시 버그 2건 수정
* **(a) 예측 안 한 AI가 최종픽으로 채워져 보이던 문제**: `MainDashboard.jsx`/`MobileDashboard.jsx`에서 빈 AI칸을 `predictedWinner`로 메꾸던 fallback 제거 → 예측 없으면 정직하게 `-` 표시.
* **(b) 베팅내역 "픽"에 팀명 대신 매치업이 표시 + 도전경기 1경기만 보이던 문제**: 화면이 옛 문자열 포맷만 파싱하고 있었음. 신규 구조 `picks=[{matchup, pick}]`를 우선 직접 사용하도록 수정(구버전 호환 유지). → 픽=팀명 정상, 도전경기 5경기 모두 표시.

### 6. 06-24 오염 데이터 청소
* **원인**: `predictions`에 같은 경기가 2벌(신규 결정ID 5개 + 옛 랜덤ID 5개) → 단독베팅 10건 중복, 옛 잔재가 신뢰도 '높음'이라 만장일치 베팅까지 생성됨.
* **해결**: 라이브 API(DELETE)로 옛 랜덤ID 예측 5개 + 06-24 베팅 12건 삭제 → 4·5단계 재실행 → 단독 5건/도전 1건/만장일치 없음으로 정상화.

### 7. 배당률 전부 1.8배 문제 — 원인 진단 완료 (해결은 내일)
* `api/odds.js`를 진단용으로 개선(에러 상태/본문 그대로 노출).
* **확정된 원인**: The Odds API가 **`INVALID_KEY` (401)** 반환 → Vercel의 `THE_ODDS_API_KEY` 환경변수 값이 잘못 등록됨. 그래서 5단계가 배당을 못 받아 전부 기본값 1.8배로 채움.

---

## ✅ 내일(06-25) 할 일
1. **배당률 키 교체 (최우선)**
   * `the-odds-api.com` 로그인(기존 계정 있으면 키 재확인, 없으면 무료 가입 — 월 500회).
   * Vercel → kbo-toto-analysis → **Settings → Environment Variables → `THE_ODDS_API_KEY`** 값을 올바른 키로 교체 후 저장.
   * Vercel **Deployments → Redeploy**.
   * 배포 후 `/api/odds` 호출해 정상 배당 들어오는지 확인 → 5단계 다시 돌려 실제 배당 반영 확인.
   * (오늘 the-odds-api 사용량이 소진되어 내일 진행하기로 함.)
2. **제미나이 429 문제 + 클로드 추가 (목표: AI 3개 완성)**
   * 최종 목표 구성: **제미나이 1 / 챗지피티 1 / 클로드 1**.
   * 제미나이 호출 수를 줄이거나(1개만 사용) 유료/한도 확인.
   * 현재 코드는 **클로드(Anthropic) 연동이 없음** → `sync.js`의 `predictWithModel`에 Claude 호출 분기 추가 필요.

### 📅 2026년 06월 19일 (오전 01:50)

### 1. 대시보드 UI 전면 개편 및 안전한 이식
* **메인 대시보드 레이아웃 교체**: 대표님께서 제시해주신 레퍼런스 사이트(https://dntprbs-maker.github.io/kbo-toto/)의 최신 디자인을 React 프로젝트(`MainDashboard.jsx`)에 성공적으로 이식했습니다.
* **적중률 / 수익률 상세 화면 구현**: 카드 클릭 시 나오는 하위 상세 화면을 레퍼런스와 동일하게 재구축했습니다. 
  * 베팅 유형별 통계 (만장일치, 단독 등)를 시각적인 막대 바(Bar) 차트로 교체했습니다.
  * AI 예측 결과 3종(반짝이, 별이, 초롱이)의 상세 표를 추가했습니다.
  * 총 투자금, 누적 손익, 종합 ROI(수익률), 총 적중 횟수 등 KPI 카드 4개를 화면 하단에 깔끔하게 배치했습니다.
* **버그 수정 및 레이아웃 안정화**: 날짜 선택기가 레이아웃 밖으로 튀어나가거나 카드가 과도하게 크게 렌더링되던 반응형(Responsive) CSS 버그를 수정하여 데스크톱(4열)과 모바일(2열)에서 모두 정상적으로 보이도록 조정했습니다.

### 2. 신규 데이터 연동 및 관리 (26.06.17 ~ 18 경기)
* **경기 결과 데이터 추가**: 6월 17일과 18일에 진행된 총 10경기의 매치업 및 최종 결과를 데이터베이스에 삽입했습니다.
* **베팅 내역 동기화**: 캡처본에 있는 만장일치, 도전경기, 단독베팅의 세부 배당률, 투자금액, 적중/미적중 상태를 정확하게 추출하여 Vercel API를 통해 백엔드(Firebase)에 등록했습니다. 
* 이를 통해 대시보드의 손익 계산기 및 경기기록/베팅내역 탭이 실제 데이터 기반으로 올바르게 렌더링되게 만들었습니다.

### 3. 세부 UI/UX 스타일 보완
* **무승부 결과 스타일링**: '결과' 표 영역에서 `무승부` 텍스트의 컬러를 기존 붉은색(미적중 처리)에서 `취소` 텍스트와 동일한 컬러(회색/주황 계열)로 통일감 있게 나오도록 렌더링 로직 및 클래스를 변경했습니다.

---

## 📅 2026년 06월 08일
*(기존 작업일지 내용 요약)*
* 상단 헤더 정렬 및 베팅 내역 PC 레이아웃 개편(좌우 나란히 배치).
* 달력(날짜 선택기) UX 개선 및 전역(Global) 필터 적용.
* 선택한 날짜 구간에 맞춘 동적 스케일 차트(Dynamic Scale Chart) 전면 재설계.
* 스마트폰 텍스트 기반 AI 관리자 자동화 시스템(V2) 구현 계획서 수립.
